let rootFileContent = "Test";
console.log(rootFileContent);

// file is ignored the file is outside the source directory and will not be compiled inside the source directory as per the changes in the tsconfig.json